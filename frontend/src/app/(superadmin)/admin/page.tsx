import { Proximamente } from "@/components/shared/proximamente";

export default function AdminHome() {
  return (
    <Proximamente
      titulo="Panel del superadministrador"
      descripcion="Gestión global de empresas, usuarios y catálogo de cursos."
      etapa="Etapa 3"
    />
  );
}
